
<%- include('partials/header') -%>
    <h1>Home</h1>
    <p> <%= content %> </p>

    <% posts.forEach(post => { %>
        <h1><%= post.title %></h1>
        <p>
        <%= post.content.slice(0, 120) + " ..." %>
        <a href="/posts/<%= post.title.toLowerCase().replace(' ', '-') %>"> Read more</a>
        </p>

    <% }) %>

<%- include('partials/footer') -%>